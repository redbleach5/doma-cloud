/**
 * Product brand — user-facing name only.
 * Technical ids (cookies, env, CSS `doma-*`, package name) stay as-is.
 */

export const BRAND_NAME = "Doma Cloud";
/** Short label for tight spaces (PWA app name, small UI). */
export const BRAND_SHORT = "Doma";
export const BRAND_TAGLINE = "домашнее хранилище";
export const BRAND_DESCRIPTION =
  "Домашнее хранилище файлов с доступом из браузера (PWA).";
/** Browser tab / OG title. */
export const BRAND_TITLE = `${BRAND_NAME} — ${BRAND_TAGLINE}`;
